import { SiteLayout } from "/components/site-layout";

const Account = () => {
  return (
    <SiteLayout pageTitle="Account">
      <h3>To be continued...</h3>
    </SiteLayout>
  );
};

export default Account;
