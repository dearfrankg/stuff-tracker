import { SiteLayout } from "/components/site-layout";

const Upload = () => {
  return (
    <SiteLayout pageTitle="Upload">
      <h3>To be continued...</h3>
    </SiteLayout>
  );
};

export default Upload;
